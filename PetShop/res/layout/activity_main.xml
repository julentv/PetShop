<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ListView
        android:id="@+id/listAnimals"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/txtAnimalName"
        android:layout_marginTop="15dp"
        android:layout_centerHorizontal="true" >

    </ListView>

    <TextView
        android:id="@+id/txtAnimalName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/listEvents"
        android:layout_alignParentTop="true"
        android:layout_marginTop="10dp"
        android:text="@string/Animal"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:textSize="18sp" />

    <EditText
        android:id="@+id/searchAnimal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/txtAnimalName"
        android:layout_marginLeft="15dp"
        android:layout_toRightOf="@+id/txtAnimalName"
        android:ems="7" />

    <Button
        android:id="@+id/btnSearch"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/txtAnimalName"
        android:layout_alignBottom="@+id/txtAnimalName"
        android:layout_alignParentRight="true"
        android:background="@drawable/ic_action_search" />

</RelativeLayout>